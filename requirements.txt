# ============================================================
# BCV · Plataforma de Conocimiento Vectorial — MVP dominio jurídico
# ============================================================
# Instalación (ver README):
#   1) torch CPU:  pip install --target .pylibs --index-url https://download.pytorch.org/whl/cpu torch
#   2) resto:      pip install --target .pylibs -r requirements.txt
# ============================================================

# --- Cliente vectorial (Weaviate) ---
weaviate-client>=4.7,<5

# --- Embeddings (bi-encoder BGE-M3) ---
sentence-transformers>=3.0
transformers>=4.44
huggingface-hub>=0.24

# --- NER zero-shot (GLiNER) ---
gliner>=0.2.13

# --- Ingesta y extracción multi-formato ---
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
pypdf>=4.0
python-docx>=1.1
openpyxl>=3.1
pandas>=2.0

# --- API ---
fastapi>=0.111
uvicorn>=0.30
pydantic>=2.7
python-dotenv>=1.0

# --- Evaluación y utilidades ---
numpy>=1.26
scikit-learn>=1.4
pytest>=8.0
